<% unless @places.img.empty? %>
  <div class="centering">
    <%= image_tag ("places/" + @places.img + ".jpg") %>
  </div>
<% end %>
<br />
<ul data-role="listview" class="ui-listview">
  <% @places.products.each do |product| %>
  <li><%= product.name %></li>
  <% end %>
</ul>
